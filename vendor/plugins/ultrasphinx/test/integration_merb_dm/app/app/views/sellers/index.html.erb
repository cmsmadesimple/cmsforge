<h1>Sellers controller, index action</h1>

<p>Edit this file in <tt>app/views/sellers/index.html.erb</tt></p>

<table>
  <tr>
  </tr>

<% for seller in @sellers %>
  <tr>
    <td><%= link_to 'Show', url(:seller, seller) %></td>
    <td><%= link_to 'Edit', url(:edit_seller,  seller) %></td>
    <td><%= delete_button :seller, seller %></td>
  </tr>
<% end %>
</table>

<%= link_to 'New', url(:new_seller) %>
