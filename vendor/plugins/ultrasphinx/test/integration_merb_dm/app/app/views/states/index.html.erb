<h1>States controller, index action</h1>

<p>Edit this file in <tt>app/views/states/index.html.erb</tt></p>

<table>
  <tr>
  </tr>

<% for state in @states %>
  <tr>
    <td><%= link_to 'Show', url(:state, state) %></td>
    <td><%= link_to 'Edit', url(:edit_state,  state) %></td>
    <td><%= delete_button :state, state %></td>
  </tr>
<% end %>
</table>

<%= link_to 'New', url(:new_state) %>
